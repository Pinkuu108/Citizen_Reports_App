package com.lb.service;

public class CitizenServiceImpl {/*
									 * implements CitizenService {
									 * 
									 * @Autowired private CitizenRepo citizenRepo;
									 * 
									 * @Override public List<String> getPlanStatus() { List<String> status =
									 * citizenRepo.getCitizenStatus(); return status; }
									 * 
									 * @Override public List<String> getPlanName() { List<String> name =
									 * citizenRepo.getCitizenName(); return name; }
									 * 
									 * @Override public List<CitizenReports> Seach(SearchRequest search) {
									 * List<CitizenReports> reports = citizenRepo.findAll();
									 * 
									 * return reports.stream() .filter(r -> search.getPlanName() == null ||
									 * r.getPlanName().equals(search.getPlanName())) .filter(r -> search.getGender()
									 * == null || r.getGender().equals(search.getGender())) .filter(r ->
									 * search.getCitizenStatus() == null ||
									 * r.getCitizenStatus().equals(search.getCitizenStatus())) .filter(r -> { if
									 * (search.getStartDate() != null && search.getEndDate() != null) { return
									 * !r.getStartDate().isBefore(search.getStartDate()) &&
									 * !r.getEndDate().isAfter(search.getEndDate()); } return true;
									 * }).collect(Collectors.toList()); }
									 * 
									 * @Override public boolean exportExcel() { List<CitizenReports> all =
									 * citizenRepo.findAll(); return false; }
									 * 
									 * @Override public boolean exportPdf() { List<CitizenReports> all =
									 * citizenRepo.findAll(); return false; }
									 */
}
